import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('Test basique vide', (WidgetTester tester) async {
    // Un test qui valide simplement que vrai = vrai
    expect(true, true);
  });
}