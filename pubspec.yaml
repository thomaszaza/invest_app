name: zaza_invest_mobile
description: "A new Flutter project."
... (le reste de ton code en haut) ...

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  fl_chart: ^0.66.0
  # ... tes autres dépendances ...

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_launcher_icons: "^0.13.1" # Vérifie bien que c'est ici

# --- CETTE PARTIE DOIT ÊTRE TOUT EN BAS, ALIGNÉE À GAUCHE ---
flutter_launcher_icons:
  image_path: "icon.png"
  android: true
  ios: true
  web:
    generate: true