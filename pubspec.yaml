name: zaza_invest_mobile
description: "A new Flutter project."
publish_to: 'none' 
version: 1.0.0+1

environment:
  sdk: ^3.13.0

dependencies:
  flutter:
    sdk: flutter
  supabase_flutter: ^2.8.0
  cupertino_icons: ^1.0.8
  fl_chart: ^1.2.0
  http: ^1.6.0
  html: ^0.15.6
  flutter_local_notifications: ^17.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  flutter_launcher_icons: ^0.14.4

# LA CONFIGURATION DE L'ICÔNE (Alignée tout à gauche)
flutter_launcher_icons:
  image_path: "icon.png" 
  android: true
  ios: true
  web:
    generate: true 

flutter:
  uses-material-design: true